name: astrbot_plugin_qwen_image_generate # 插件唯一识别名，以 astrbot_plugin_ 前缀开头
display_name: qwen-image-generate(千问生图) # 用于展示的名字，可以是方便人类阅读的名字（需要版本 >= v4.5.0，低版本不会报错，请放心填写）
desc: 通过千问的api生图 # 插件简短描述
version: v1.0.0 # 插件版本号。格式：v1.1.1 或者 v1.1
author: lbh # 作者
astrbot_version: ">=4.16"
repo: https://github.com/kikikkii/astrbot-qwen-image.git # 插件的仓库地址
